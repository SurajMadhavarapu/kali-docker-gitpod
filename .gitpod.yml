image:
  file: Dockerfile

tasks:
  - init: apt-get update && apt-get install -y xfce4 xfce4-goodies tightvncserver novnc
  - command: vncserver :1 -geometry 1280x720 -depth 24 && websockify --web=/usr/share/novnc/ 6080 localhost:5901
